// Stub for `ai` package — the `agents` MCP code dynamically imports it for
// non-MCP features we don't use. Aliased via wrangler.toml [alias].
export const jsonSchema = (s) => s;
export default {};
